# Database Connection
# Railway will automatically provide DATABASE_URL if using Railway PostgreSQL
# For local development, use your local PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/payroll_system?schema=public"

# Server Configuration
PORT=3001
NODE_ENV=production

# Security - JWT Secret
# Generate a strong random string for production (e.g., openssl rand -base64 32)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# CORS Configuration
# Comma-separated list of allowed origins
# Example: "https://your-frontend.railway.app,https://your-custom-domain.com"
CORS_ORIGINS="http://localhost:5173,http://localhost:5174,http://localhost:5175"
